# Configuración del servidor
PORT=3000
NODE_ENV=development

# ═══════════════════════════════════════════════════════════════════════
# Configuración de la base de datos
# ═══════════════════════════════════════════════════════════════════════

# OPCIÓN 1: Docker Compose (RECOMENDADO)
# Si usas docker-compose up -d, usa estas credenciales:
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/api_productos?schema=public"

# OPCIÓN 2: PostgreSQL local (Homebrew en macOS)
# Si instalaste PostgreSQL con brew, descomenta y usa tu usuario:
# DATABASE_URL="postgresql://tu_usuario@localhost:5432/api_productos?schema=public"

# OPCIÓN 3: PostgreSQL local con contraseña
# DATABASE_URL="postgresql://usuario:password@localhost:5432/api_productos?schema=public"

# OPCIÓN 4: MySQL
# DATABASE_URL="mysql://usuario:password@localhost:3306/api_productos"

# ═══════════════════════════════════════════════════════════════════════
# Configuración de JWT
# ═══════════════════════════════════════════════════════════════════════

# IMPORTANTE: En producción, usa una clave secreta fuerte y única
JWT_SECRET=mi_clave_secreta_super_segura_cambiar_en_produccion_2024
JWT_EXPIRE=7d

# ═══════════════════════════════════════════════════════════════════════
# Configuración de CORS (opcional)
# ═══════════════════════════════════════════════════════════════════════

# Lista de orígenes permitidos separados por comas
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# Para permitir todos los orígenes (solo desarrollo):
# ALLOWED_ORIGINS=*
